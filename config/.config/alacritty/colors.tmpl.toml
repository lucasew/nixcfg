# Auto-generated from template by workspaced

[colors.primary]
background = "#{{ (index (index .Modules "base16") "base00") }}"
foreground = "#{{ (index (index .Modules "base16") "base05") }}"

[colors.cursor]
text   = "#{{ (index (index .Modules "base16") "base00") }}"
cursor = "#{{ (index (index .Modules "base16") "base05") }}"

[colors.normal]
black   = "#{{ (index (index .Modules "base16") "base00") }}"
red     = "#{{ (index (index .Modules "base16") "base08") }}"
green   = "#{{ (index (index .Modules "base16") "base0B") }}"
yellow  = "#{{ (index (index .Modules "base16") "base0A") }}"
blue    = "#{{ (index (index .Modules "base16") "base0D") }}"
magenta = "#{{ (index (index .Modules "base16") "base0E") }}"
cyan    = "#{{ (index (index .Modules "base16") "base0C") }}"
white   = "#{{ (index (index .Modules "base16") "base05") }}"

[colors.bright]
black   = "#{{ (index (index .Modules "base16") "base03") }}"
red     = "#{{ (index (index .Modules "base16") "base08") }}"
green   = "#{{ (index (index .Modules "base16") "base0B") }}"
yellow  = "#{{ (index (index .Modules "base16") "base0A") }}"
blue    = "#{{ (index (index .Modules "base16") "base0D") }}"
magenta = "#{{ (index (index .Modules "base16") "base0E") }}"
cyan    = "#{{ (index (index .Modules "base16") "base0C") }}"
white   = "#{{ (index (index .Modules "base16") "base07") }}"
