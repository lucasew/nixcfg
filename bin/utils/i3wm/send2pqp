#!/usr/bin/env bash

wsfile="/tmp/_i3_last_ws"

last_ws=10
if [ -f $wsfile ]; then
  last_ws="$(cat "$wsfile")"
fi
last_ws=$((last_ws+1))
echo $last_ws
echo $last_ws > $wsfile

i3-msg move container to workspace number $last_ws
i3-msg workspace number $last_ws
