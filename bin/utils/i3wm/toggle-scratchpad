#!/usr/bin/env bash

function rpc-msg {
	if [ -v WAYLAND_DISPLAY ]; then
		sway-msg "$@"
	else
		i3wm "$@"
	fi
}

sd utils i3wm rpc scratchpad show
workspaced dispatch brightness show
workspaced dispatch audio show
workspaced dispatch media show
