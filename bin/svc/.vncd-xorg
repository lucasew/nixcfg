#!/usr/bin/env -S sd nix shell
#!nix-shell -i bash -p tigervnc
#! vim:ft=bash

# Always require a password for VNC.
# Relying solely on network-level security (like a VPN) is not sufficient.
# This follows the principle of "defense in depth". If the network layer is ever
# compromised, the service itself should still be secure.
x0vncserver -display=:0 -PasswordFile ~/.vnc/passwd -ImprovedHextile=1 -RawKeyboard=1
