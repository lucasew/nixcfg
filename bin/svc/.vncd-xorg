#!/usr/bin/env -S sd nix shell
#!nix-shell -i bash -p tigervnc
#! vim:ft=bash

# VNC password authentication is required.
# Before running this script, you must create a VNC password file using the vncpasswd utility:
#
#   vncpasswd ~/.vnc/passwd
#
# The VNC server will then use this file to authenticate clients.
# While the VNC port (5900) should ideally be restricted to a secure network (e.g., Tailscale),
# enforcing password authentication provides an essential layer of defense-in-depth.
x0vncserver -display=:0 -PasswordFile ~/.vnc/passwd -ImprovedHextile=1 -RawKeyboard=1
