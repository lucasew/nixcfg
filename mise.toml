# Warning: Do not unify this file with others. Keep tool definitions localized.
[tasks.lint]
description = "Run all linters"
run = "workspaced codebase lint"

[tasks.fmt]
description = "Format all files"
run = "workspaced codebase format"

[tasks."test:fallback"]
description = "Dummy task for test wildcard"
run = "true"

[tasks.test]
depends = ["test:*"]

[tasks."codegen:fallback"]
description = "Dummy task for codegen wildcard"
run = "true"

[tasks.codegen]
depends = ["codegen:*"]

[tasks."install:fallback"]
description = "Dummy task for install wildcard"
run = "true"

[tasks.install]
depends = ["install:*"]

[tasks.ci]
description = "Run CI pipeline locally"
depends = ["lint", "test"]

[tools]
# "github:lucasew/workspaced" = "0.2.1"
sops = "3.11.0"
